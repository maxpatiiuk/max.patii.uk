// Not supporting .root customization, so root is always process.cwd()
export const root = `${process.cwd()}/`;
export const fullPublicDir = `${root}public/`;
export const outDirectory = 'dist/';
export const fullPagesDirectory = `${root}src/pages/`;
export const fullCollectionsPath = `${root}src/collections.ts`;
